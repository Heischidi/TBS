"use client";

export function CartProvider({ children }: { children: React.ReactNode }) {
  // Cart state is managed by Zustand store (persisted)
  // This provider is a placeholder for any future context needs
  return <>{children}</>;
}
